# standpoint — development & testing dependencies.
#
#     pip install -r requirements-dev.txt
#
# Derived from pyproject.toml's [dev] and [gui] extras -- the same combination
# CI installs from (`pip install -e ".[dev,gui]"`). Kept only for contributors
# who prefer `pip install -r requirements-dev.txt` -- edit the extras in
# pyproject.toml, not this file, to change what installs. The heavier, opt-in
# bits stay out of here:
#   - DeepEval (tests/test_eval.py)      -> pip install ".[eval]"   (needs Ollama)
#   - fastapi-mcp (tests/test_mcp.py)    -> pip install ".[mcp]"
#   - Playwright (tests/test_gui_e2e.py) -> pip install playwright && playwright install chromium
-e .[dev,gui]
ruff==0.15.21
